#include "main.h"
/**
*_puts- function that returns the length of a string
*@str: string to swap
*Return: String in reverse
**/
void _puts(char *str)
{
int i;
for (i = 0; i[str] != '\0'; i++)
{
_putchar(str[i]);
}
_putchar(10);
}
