#include "main.h"
/**
*_strlen - function that returns the length of a string
*@s:string
*Return: Length of string
**/
int _strlen(char *s)
{
int length = 0, i;
for (i = 0; i[s] != '\0'; i++)
{
length++;
}
return (length);
}
