#include "main.h"
/**
*_abs -  function that computes the absolute value of an integer.
*@value: Number in question
*Return: positive number by negating it
**/
int _abs(int value)
{
if (value < 0)
{
value = -value
}
return (value);
}
