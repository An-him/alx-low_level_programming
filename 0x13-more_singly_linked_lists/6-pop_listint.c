#include "lists.h"
/**
*pop_listint - function that deletes the head node
*@head: pointer to head
*Return: int
**/
int pop_listint(listint_t **head)
{
int result;
result = 0;
if (*head == NULL)
{
return (result);
}
else
return (result);
}
